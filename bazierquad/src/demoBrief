一、Demo作用
使用二阶贝塞尔曲线

二、制作流程
1、确定贝塞尔曲线的三个点，并绘制固定的贝塞尔曲线。
过程中发现，三个点的坐标，一个一个标上去太麻烦了，就创建一个Point类来设置坐标。
而且容易分不清点（创建Point类，并重构）

2、重写onTouchEvent()然后改变Control点

三、知识点
贝塞尔曲线有三个点。在绘制的时候第一个点是moveTo()。
然后才是quadTo()。