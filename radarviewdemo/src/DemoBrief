一、Demo作用
绘制雷达图，可用于属性的显示

二、制作流程
1、先画出一个六边形
2、画出一堆六边形
3、绘制文字
4、绘制点
5、绘制区域

三、遇到的问题
1、Math.cos()是弧度制的，不是角度制的。
回答：也就是输入参数是弧度2π，不是角度360。

四、可变参数
1、绘制多少个六边形
2、第一个六边形的半径
3、多个六边形的间距
4、设置文字、每个位置的属性值(这个做了，其他的都没弄- -)
5、设置点的大小。
6、设置全部的背景颜色。。。

五、未完成的部分
1、文字的位置调整（不想改了，额。。。好懒）
